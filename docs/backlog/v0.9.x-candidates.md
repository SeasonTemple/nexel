---
date_created: 2026-05-29
last_updated: 2026-05-29
status: rolling
scope: post-v0.9.0 candidates (v0.9.1 / v0.10.0 / v1.0+ planning)
---

# v0.9.x backlog candidates

Single rolling index of deferred items across all sources. Maintain by
appending; do not delete entries — strike through with `~~done in vX.Y.Z~~`
when shipped.

> **2026-05-29 reconciliation audit.** This index was first assembled by
> carrying forward the 2026-05-28 ideation ranking *without* checking it
> against what already shipped in v0.8.x/v0.9.0. A code audit found the
> top-ranked v0.10.0 candidates were already delivered, and the v0.9.1
> "hygiene" items are non-defects. Corrections are inline below
> (strikethrough + `[AUDIT]` notes). **Re-rank from the verified-remaining
> set, not from the original Rank N order.**

Source legend:
- **RN** — referenced in release notes (`docs/release-notes/v*.md`)
- **ADR** — referenced in an ADR Consequences/Rejected section
- **IDE** — ideation doc `docs/ideation/2026-05-28-netops-skills-absorption-ideation.md`
- **REV** — Phase 4 release-gate review finding (P2/P3) not fixed in same PR
- **CLAUDE** — `CLAUDE.md` deferred note

## v0.9.1 candidates — Phase 4 review carry-over (P2/P3 hygiene)

Small mechanical fixes deferred from Phase 4 fix-loop. Each is < 50 LOC.

> **[AUDIT 2026-05-29] These five are non-defects, not a release.** Code
> inspection of the "correctness" subset:
> - **#9 seam-collapse trailing newline** — unreachable via nexel's own
>   write paths. `writeAmbientFence` always emits the `\n\n`/`\n` separators,
>   so a nexel-written fence removed by `removeAmbientFence` always leaves the
>   file ending in exactly one `\n` (or empty). The missing-`\n` case needs an
>   externally hand-edited file that *also* preserves the managed-header
>   sentinel — contrived, and cosmetic even when hit.
> - **#15 `readBlessMarker` micro-TOCTOU** — real ms window, but requires an
>   attacker with write access to `.nexel/` on the maintainer's release
>   machine mid-verification. Threat model is empty; ADR-0019 parity is
>   aesthetic.
> - **#8 `ToctouError`** — `error-format.mjs` serializes by `.code`, which the
>   plain `Error` already carries, so the envelope is already correct. Value
>   is consistency + structured `.details`, *not* "serialization fidelity."
> - **#5 `${appliesTo}s` pluralization** — today `appliesTo` ∈ {`manifest`
>   (special-cased, never pluralized), `skill` (→ `skills`, correct)}. Zero
>   live miss; pure future-proofing.
> - **#11 `deactivateCommand`/`activateCommand` scope validation** — the
>   commands are not in the `index.mjs` public barrel and `cli/**` is not
>   downstream-importable, so the only caller (`runDeactivate`/`runActivate`)
>   already validates via `resolveScope`. The unvalidated path is
>   architecturally unreachable.
>
> **Disposition:** do NOT cut a v0.9.1 release for these. Keep as documented
> known-limitations and fix opportunistically when the relevant file is
> touched for real work (#5 when a new `appliesTo` value lands; #11 if `cli/`
> ever goes public; #9 if a real corruption report appears). The only item
> with arguable proactive value is **#14** (the bless-bypass regex test),
> because it guards a *security control* — but the honest fix is YAML-extract
> + shell-out to real `grep -E` (POSIX `[[:space:]]` is not valid JS regex),
> not the mechanical substring swap the one-liner implies.

### ADR-0016 Validator Registry (PR #10)
- **REV/P2** — `ctx.productConfig` shallow-frozen but reference is mutable; document or deep-freeze copy. `core/manifest/validator.mjs:85`.
- **REV/P2** — `_resetRegistryForTests` exported from `core/registry.mjs`; convention (`_` prefix) is sole guard against downstream products importing core directly. Move to a separate `registry.testutil.mjs` OR add a `process.env.NODE_ENV === "test"` guard.
- **REV/P2** — Cascade behavior: rules run on subjects already flagged by built-in checks. Either document or skip when prior finding targets same `section:id`.
- **REV/P2** — `withRegistryReset` test pattern at `validator.test.mjs:155-160` is fragile under `node:test` concurrency; lock per-call dependency injection OR set `concurrency: false`.
- **REV/P3** — `${rule.appliesTo}s` implicit pluralization (`validator.mjs:103`); future `policy` → `policys` silent miss. Use explicit map.

### ADR-0019 TOCTOU (PR #11)
- **REV/P2** — `withPatchedLockSync` test helper global monkey-patches `lockfile.lockSync`; thrown `injectDuringLock` can leak the patch. Scope by `t.before`/`t.after` hooks.
- **REV/P2** — FAT32/exFAT degradation (Stats.ino === 0) acknowledged in ADR-0019 but no test. Add a smoke test that mocks `lstatSync.ino = 0` and asserts symlink-only behavior.
- **REV/P3** — `verifyPostLockIntegrity` throws plain `new Error()` with `.code`; introduce `class ToctouError extends Error` for ADR-0014 envelope serialization fidelity.

### ADR-0017 deactivate (PR #12)
- **REV/P2** — Seam-collapse asymmetric trailing newline (when fence at file-end). After splice, file may end without `\n`. Normalize to always end with exactly one `\n` when content remains.
- **REV/P2** — No concurrent-lock test for `removeAmbientFence`. Add ELOCKED → ERR_LOCKED case (pre-creates lock dir).
- **REV/P2** — `deactivateCommand` does not validate `scope`; only `runDeactivate` does. Same gap exists in `activateCommand`. Paired fix.
- **REV/P2** — `createBufferedLogger` duplicated verbatim from `activate.mjs`. Hoist into shared module (`cli/commands/_logger.mjs` or similar).

### ADR-0018 CI bless (PR #8)
- **REV/P2** — Test fixture default `{GITHUB_ACTIONS: "true", NEXEL_CI_BLESSED: "true"}` masks future signal requirements. Prefer marker fixtures for happy-path tests; reserve env-only paths for dedicated ADR-0018 cases.
- **REV/P2** — `release.yml.test.mjs` substring-asserts the bypass-scan needles; does not validate the regex anchors. Test against commit fixtures pass/fail directly.
- **REV/P2** — `readBlessMarker` has `existsSync` + `lstatSync` micro-TOCTOU. Use single `lstatSync` + ENOENT catch (mirrors ADR-0019 `snapshotPreLockStats` pattern after Phase 4 fix).

### Cross-cutting harness
- **RN** — Confirm harness `GIT_DIR` scrub pattern is documented and applied to any new hooks added in v0.9.x. Reference commit: `a6c69a1`.

## v0.10.0 candidates — ideation ranked survivors

From `docs/ideation/2026-05-28-...md` post-grill ranking. v0.9.0 covered
Idea 2 (Validator Registry portion) + Idea 5-TOCTOU (partial).

> **[AUDIT 2026-05-29] The "6 remain" count and the Rank order are wrong.**
> Verified against shipped code: **2 are already delivered or near-done**
> (Idea 4a shipped v0.8.0; Idea 5-ladder ~80% present), and the "Idea 5"
> label collides — the ideation doc used it for *both* the TOCTOU work
> (v0.9.0 partial) *and* the state-migration ladder (Rank 2 here). Treat them
> as distinct: **Idea 5-TOCTOU** vs **Idea 5-ladder**. Verified-remaining set
> is below the audit notes.

### ~~Rank 1 — Idea 4a: Plugin Runtime 横向扩展~~ — **done in v0.8.0**
- **[AUDIT] SHIPPED.** `scripts/installer/adapters/claude-plugin.mjs` +
  `codex-plugin.mjs` exist, are fully wired into the `activate` verb
  (`cli/commands/activate.mjs:193,207` call `activateClaude`/`activateCodex`),
  carry dedicated test files in the suite, and are documented in release notes
  v0.8.0–v0.8.3 + ADR-0011/0012. The "install-plane/activation-plane split
  only OpenCode-implemented" premise was already false when this index was
  written. No ADR-0020 needed for this; strike it.
- ~~Original: Add claude-plugin.mjs + codex-plugin.mjs mirroring opencode-plugin.mjs (ADR-0009 subpath-export pattern). Likely ADR-0020.~~

### Rank 2 — Idea 5-ladder: State schema-migration ladder (Conf 85%, Complexity ~~Med~~ **Low** post-audit)
- **[AUDIT] ~80% already present.** `core/state.mjs` has the full walker:
  `migrateState` iterates a `MIGRATIONS` table from `schemaVersion` up to
  `MAX_SUPPORTED_SCHEMA`, with the complete error taxonomy
  (`ERR_NO_MIGRATION` / `ERR_BAD_MIGRATION` / `ERR_SCHEMA_TOO_NEW`). The
  versioned-schema field exists as `schemaVersion` (not the proposed
  `stateSchema`). What actually remains is small: (a) `const MIGRATIONS = {}`
  is empty — no migration registered yet (none needed until the first schema
  bump), and (b) it's a module-private const with no downstream/registration
  seam. So this is NOT a "build the ladder" project; it's "populate + expose
  a registration point when the first real schema change arrives." Re-scope
  accordingly; likely no standalone ADR until a concrete migration forces one.
- ~~Original: state.json adds stateSchema:N; kernel walks registered v(N)→v(N+1) migrations. Build now. Likely ADR-0021.~~

### Rank 3 — Idea 3: Optional staged content pipeline SPI (Conf 85%, Complexity Med)
- **IDE** — SPI v1.3 adds optional `contentPipeline?: Stage[]`. Coexists with v1.2 `transformAssetContent?` (auto-promotes single hook to 1-stage pipeline). Additive minor per ADR-0010.
- Unlocks: third-party stage packs, self-improve hook landing point, lint as a stage.
- Likely ADR-0022.

### Rank 4 — Idea 1: Skill Vendoring + `@nexel/vendor` 姊妹包 (Conf 85%, Complexity Med, kernel zero changes)
- **IDE** — Sister package `@nexel/vendor` declarative `vendorFrom` + `.vendored.json` sidecar + `vendor {pull|diff|fork}` CLI. Kernel zero changes (consumes public API only).
- Blocked on: kernel public API publish (ADR-0005 v1.0 clock) — but design + scaffold can land before adoption.

### Rank 6 — Idea 6: Capability handshake + conformance + doctor probes (Conf 80%, Complexity Med)
- **IDE** — Three-piece: adapter `capabilities()`; `@nexel/adapter-conformance` published package; doctor `probes()` SPI.
- Enables SPI evolution via capability bits instead of break-all-adapters.
- **[AUDIT 2026-05-29] Partially present — re-scope to the missing third.**
  `doctorCommand` already exists and returns a structured health report
  (`cli/commands/index.test.mjs:164`), and an internal `conformance.test.mjs`
  already runs in the suite. Genuinely missing: (1) the adapter
  `capabilities()` bit-handshake on the SPI (SPI is at v1.2; no capability
  surface), (2) `@nexel/adapter-conformance` as a *published* package (today
  it's an in-repo test, not a consumable package), (3) a doctor `probes()`
  SPI letting adapters contribute probes. The "three-piece, all-new" framing
  overstates the gap.
- **[UPDATE 2026-05-29] Piece (3) wired on `main`** (uncommitted at time of
  writing): `doctorCommand` (`cli/commands/index.mjs`) now folds each adapter's
  `doctorProbes({targetRoot, env, productConfig})` into the report, with
  fail-soft handling for throwing/malformed/non-array returns; +5 tests in
  `cli/commands/index.test.mjs`. Closed the tested-but-dead seam. Pieces (1)
  `capabilities()` and (2) published conformance package remain deferred
  (speculative / ADR-0005-gated). No built-in adapter defines a real probe yet
  — that is a separate deliberate decision, intentionally not gold-plated.

### Rank 7 — Idea 7: Manifest from filesystem 派生 (Conf 80%, Complexity Med)
- **IDE** — `nexel rebuild-manifest` walks `<skillsDir>/*/SKILL.md`, emits canonical `install.json`. `lint:manifest` becomes regen-and-diff. `ProductConfig.manifestMode: "authored" | "derived"`.
- Removes the double-encoding (frontmatter ↔ manifest) friction.

### Rank 8 — Idea 4b: Multi-tenant install plane + `runTransactional` (Conf 75%, Complexity Med-High)
- **IDE** — `<target>/.nexel/host-registry.json` + new verbs `nexel hosts` / `nexel host gc`. Public `runTransactional({steps, onRollback})` API.
- Distribution-scale enabler. Lower confidence; defer until adoption signal.

## v1.0 + ADR-related deferred

### Multi-product / `--all-products` flag (ADR-0017 follow-up)
- **ADR/RN** — Needs on-disk product registry. Either (a) host-registry from Idea 4b, or (b) wildcard begin-marker scan with confirmation. Own ADR territory.

### Windows CI matrix (ADR-0019 follow-up)
- **ADR/RN** — ADR-0019 verified theoretical cross-platform via Stats.ino semantics. Actual Windows runner in `.github/workflows/ci.yml` deferred until: (a) Windows adopter surfaces, or (b) routine cross-platform regression policy is documented.

### npm publish (ADR-0005)
- **ADR/RN** — v1.0 clock starts when ≥ 1 production downstream adopter runs nexel a full quarter. Independent of v0.9.x work.

### CODEOWNERS + branch protection (ADR-0018 follow-up)
- **ADR/RN** — Required mitigations against `.github/workflows/**` mutation bypass. Lives in GitHub repo settings (CODEOWNERS file + branch protection rules), not in code. Recommend before any external contributor is granted push.

### ADR-0013 / ADR-0018 hardening
- **RN** — Consider raising CI gate beyond deterministic checks: shell out to a hosted `ce-code-review` mode in CI (cost vs latency trade-off). Not strictly required while maintainer-machine path is primary.

## ADR-0004 historical (CLAUDE.md note)

- **CLAUDE** — Typed-error loader layer (loader.mjs is pure IO+parse by design; typing belongs in validator/CLI layer's job).
- **CLAUDE** — `renderNextSteps` stale product-specific bin-path literal (needs ProductConfig/caller seam).

## Triage notes

> **[AUDIT 2026-05-29] The triage notes below are superseded.** They
> recommended Idea 4a (already shipped v0.8.0) and Idea 5-ladder (~80%
> present). Use the **Verified-remaining set** instead. Original notes kept
> struck-through for archaeology.

### Verified-remaining set (post-audit, re-ranked by genuine remaining work)

Confirmed against shipped code — these were actually not built:

> **[DELIVERED 2026-05-29] The high-value half of this set has shipped to
> `main`.** Items 1–3 are done; only the speculative / adoption-gated tail
> (4–6) remains. Do NOT carry 1–3 forward as candidates.

1. ~~**Idea 3 — contentPipeline SPI**~~ — **SHIPPED** (ADR-0020, `adapters/spi.mjs`
   v1.3 `contentPipeline`). Done before this session.
2. ~~**Idea 7 — `rebuild-manifest`**~~ — **SHIPPED 2026-05-29** (`scripts/rebuild-manifest.mjs`
   commits `d95f6a8`+`c24ff95`; plan `docs/plans/2026-05-29-003-...`). `manifestMode`
   deliberately dropped (unenforced field = dead contract surface; revisit with an
   ADR if kernel-side enforcement ever lands).
3. ~~**Idea 1 — `@nexel/vendor` sister package**~~ — **MVP SHIPPED 2026-05-29**
   (`examples/vendor-tool/`, commit `49480aa`; plan `docs/plans/2026-05-29-002-...`).
   pull/diff/fork, local-path source, kernel zero-import. git/registry sources, 3-way
   merge, npm publish remain deferred (README known-limitations; publish ADR-0005-gated).
4. **Idea 6 — capability handshake** (partial: doctor + `doctorProbes` SPI +
   internal conformance exist; missing the `capabilities()` bits and a *published*
   conformance package). Re-scope to those two. **Speculative — no consumer
   pushing; YAGNI risk. Defer.**
5. **Idea 4b — multi-tenant install plane + `runTransactional`** (zero code).
   Lowest confidence; defer until adoption signal.
6. **Idea 5-ladder — small remainder only** (populate `MIGRATIONS` + expose a
   registration seam when the first real schema bump lands; not a standalone
   project today).

~~- **Smallest viable v0.9.1**: bundle the P2/P3 hygiene items into one `chore(core+release): post-v0.9.0 review carry-over` PR. ~150 LOC across 4-5 files. No new ADR.~~ (Audit: these are non-defects; no v0.9.1 warranted.)
~~- **Best single v0.10.0 feature**: Idea 4a (Plugin Runtime 横向扩展). 90% confidence, Low-Med complexity, no ADR breakage.~~ (Audit: shipped v0.8.0.)
~~- **Best v0.10.x infra investment**: Idea 5 (migration ladder). Pre-1.0 unfreeze budget is cheapest now; bridges all future state-schema changes.~~ (Audit: ladder mechanism already in `state.mjs`; only a small remainder left.)
~~- **Best v0.11+ ecosystem move**: Idea 1 (`@nexel/vendor` sister package).~~ (Shipped 2026-05-29, MVP — commit `49480aa`.)

> **[2026-05-29] Verified-remaining is now exhausted of high-value work.** Idea
> 1/3/7 shipped. The tail (Idea 6 `capabilities()` + published conformance pkg,
> Idea 4b multi-tenant, npm publish, vendor git/registry sources) is all
> speculative or ADR-0005 adoption-clock-gated. Per
> [[feedback-audit-backlog-against-code]], do not build these without an
> adoption signal — re-audit against shipped code before picking anything here.

## Maintenance protocol

When a release ships:
1. Strike through completed items here with `~~done in vX.Y.Z~~`.
2. Add new deferred items from the release notes + Phase 4 review.
3. Re-rank `v0.9.x candidates` / `v0.10.0 candidates` sections if confidence shifts.
4. Leave historical items in place — they document context for future archaeology.
