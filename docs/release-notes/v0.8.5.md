# v0.8.5 — Symlink-at-lock DOS defense + ADR-0014 dead-fallback removal

> 2026-05-29 · adv-r5-C closure + ERR_ACTIVATE_FAILED cleanup + ADR-0015 · 456 / 456 tests passing

Round-6 closes the symlink-at-`<target>.lock` DOS class (caught by v0.8.4
pre-commit review) and removes ADR-0014's unreachable
`ERR_ACTIVATE_FAILED` fallback. Adds ADR-0015 for the v0.8.4→v0.8.5
back-to-back add/remove of that constant.

## 中文

### Highlights

| 维度 | 改动 | 为什么重要 |
|---|---|---|
| **adv-r5-C symlink-at-lock DOS 防御** | `writeAmbientFence` lstats `<target>.lock` before lockSync；symlink → refuse with actionable error | 攻击者 (或残留状态) 在 `.lock` 位置 plant 一个 symlink (`ln -s /any/file <target>.lock`) → proper-lockfile 的 stale cleanup 不动 symlink → 所有 subsequent activate 永久 block 直到手工清。v0.8.3 symlink-at-target 同类，此处补 lock path |
| **ERR_ACTIVATE_FAILED 删 (ADR-0015)** | v0.8.4 ADR-0014 引入作为 unification fallback；v0.8.5 round-6 audit 确认 `activateCommand` 永远 push refused/failed entry 时 ok:false → fallback unreachable。Constant 从 errors.mjs + index.mjs export + runActivate fallback 删 | Dead code 清；runActivate 用显式 contract assertion 替代 (TypeError 改为 actionable error message) |
| **AGENT-CLI-CONTRACT doc + ADR-0015** | activate subsection 改：`error` 字段总是 specific per-adapter code (不再说 fallback 存在)。ADR-0015 记录 add/remove churn | ADR-0014 保持 v0.8.4 历史；ADR-0015 amend |
| **Memory `feedback_pre_stop_audit.md`** | Session-pattern fix: 6 connection "default 收 → user push" 证明我有 stop-bias。新 memory rule：声明 done 前 audit 每个 defer (genuine design call vs session-fatigue mechanical) | Code-quality 由 ADR-0013 husky gate enforce；session-scoping 由 memory rule + self-discipline enforce |
| **Test message anchoring** | symlink-at-lock 测试现在 assert distinctive guard wording (`planted DOS or stale state`)，不仅 generic `/symlink/i` | 锁定 v0.8.5 守门，防 future 改 lockSync vendor 行为蒙混 |

### Compatibility

| What | Before (v0.8.4) | After (v0.8.5) | Action |
|---|---|---|---|
| `ERR_ACTIVATE_FAILED` 公共导出 | Present (re-exported from index.mjs) | Removed | **Breaking** for v0.8.4 consumers pinning the constant import. Zero-adoption premise (ADR-0005/0008) holds; v0.8.4 shipped <1 day ago |
| `writeAmbientFence` 行为 | Vulnerable to planted-symlink-at-lock DOS | Refuses with actionable error | 用户 file 干净时零变化；存在恶意 / 残留 symlink at `.lock` 时 fail-loud + 指出该删什么 |
| `runActivate` failure path | Defensive `?? "ERR_ACTIVATE_FAILED"` fallback | Explicit contract assertion (loud error on violation) | 行为完全相同 (fallback 不可达);  代码更严格 |

### Closed in this release

| Ref | Finding | Source |
|---|---|---|
| adv-r5-C | symlink-at-`<target>.lock` DOS — planted symlink permanently blocks activate | v0.8.4 pre-commit adversarial |
| CR-R4 / cleanup | ERR_ACTIVATE_FAILED unreachable defensive fallback | v0.8.4 pre-commit correctness |
| adv-r6-1 (本轮) | docs drift: ADR-0014 + AGENT-CLI-CONTRACT 引用已删的 constant | v0.8.5 pre-commit adversarial |
| correctness-2 (本轮) | removed defensive fallback 留 invariant 未 assert | v0.8.5 pre-commit correctness |
| correctness-3 (本轮) | symlink-at-lock test 不锁定 v0.8.5 guard | v0.8.5 pre-commit correctness |

合计：**456 / 456 tests passing** (v0.8.4 455 → v0.8.5 456 net +1: 新 symlink-at-lock test)。

### Memory rule (session-discipline)

`feedback_pre_stop_audit.md` 加入 agent memory: 声明 session done 前必须 audit 每个 deferred item，区分:
- **Genuine defer**: 需 ideation / ADR / 外部输入 / 真设计决策
- **Session-fatigue defer**: 机械 ~30 LOC 改动 + 测试，无 alternatives 要权衡

Session-fatigue defer 必须本轮做完。Across v0.8.x cycle (6 rounds: ideation → v0.8.4 → v0.8.5)，我声明 stop 6 次；每次 user push back 抓 2-5 mechanical items。新 rule + 已建 ADR-0013 husky gate (代码层) + 已建 `feedback_review_before_push.md` (review timing) 构成完整 session-discipline triad。

### Deferred (round-7 候选)

| Item | 为什么真 defer |
|---|---|
| adv-009 deactivate verb | 新 feature 需 ideation (cleanup 语义 / state model) |
| CI bless path | server-side hook vs encrypted marker vs --no-verify policy — 真设计选择 |
| 3 ADR drafts (Validator Rule Registry / Migration Ladder / contentPipeline) | ADR-0001 D1 implementation-time |
| TOCTOU window 在 lstat/lockSync 之间 | 真 design — O_NOFOLLOW Linux-only，需 cross-platform 策略 |

剩 4 项 backlog **全部真 defer** (pre-stop audit pass)。Session-fatigue 桶 empty。

### Notes

- v0.8.5 push 用 `npm run release:bless -- v0.8.5` (ADR-0013 gate dogfooding round 3 — 三连 clean ship through gate)
- ADR-0015 supersedes nothing; ADR-0014 保持 v0.8.4 历史。v0.8.5 是 amend
- 6 release in session (v0.8.0/8.1/8.2/8.3/8.4/8.5)；最后 3 个 (v0.8.3/8.4/8.5) 全是真 pre-commit review

---

## English

### Highlights

| Area | Change | Why it matters |
|---|---|---|
| **adv-r5-C symlink-at-lock DOS defense** | `writeAmbientFence` lstats `<target>.lock` before lockSync; symlink → refuse with actionable error | A planted symlink at the lock path (`ln -s /any/file <target>.lock`) defeats proper-lockfile's stale cleanup (symlinks are never auto-removed) and permanently DOS's every subsequent activate. v0.8.3 closed symlink-at-target; v0.8.5 closes the lock-path variant |
| **ERR_ACTIVATE_FAILED removed (ADR-0015)** | v0.8.4 ADR-0014 introduced it as a unification fallback; v0.8.5 round-6 audit confirmed `activateCommand` always pushes a refused/failed entry when flipping ok:false → fallback unreachable. Constant removed from errors.mjs + index.mjs + runActivate | Dead code cleanup; runActivate replaces the fallback with an explicit contract assertion (loud actionable error instead of silent TypeError) |
| **AGENT-CLI-CONTRACT + ADR-0015** | activate subsection updated: `error` field always populated from a specific per-adapter code (no fallback mention). ADR-0015 records the v0.8.4 → v0.8.5 add/remove churn | ADR-0014 stays as v0.8.4 historical record; ADR-0015 is the amend |
| **Memory `feedback_pre_stop_audit.md`** | Session-pattern fix: 6 consecutive "default stop → user pushback" cycles proved stop-bias. New memory rule: audit every defer (genuine design call vs session-fatigue mechanical) before declaring done | Code-quality enforced by ADR-0013 husky gate; session-scoping now enforced by memory rule + self-discipline |
| **Test message anchoring** | symlink-at-lock test now asserts the v0.8.5 guard's distinctive wording ("planted DOS or stale state"), not just generic `/symlink/i` | Pins the v0.8.5 defense; a future change to lockSync vendor behavior can't quietly subsume the assertion |

### Compatibility

| What | Before (v0.8.4) | After (v0.8.5) | Action |
|---|---|---|---|
| `ERR_ACTIVATE_FAILED` public export | Present (re-exported from index.mjs) | Removed | **Breaking** for v0.8.4 consumers importing the constant. Zero-adoption premise (ADR-0005/0008) holds; v0.8.4 shipped <1 day ago |
| `writeAmbientFence` behavior | Vulnerable to planted-symlink-at-lock DOS | Refuses with actionable error | Unchanged for clean files; fails loud (with cleanup instruction) when malicious/stale symlink exists at `.lock` |
| `runActivate` failure path | Defensive `?? "ERR_ACTIVATE_FAILED"` fallback | Explicit contract assertion (loud error on violation) | Same runtime behavior (fallback was unreachable); code is now stricter |

### Closed in this release

| Ref | Finding | Source |
|---|---|---|
| adv-r5-C | symlink-at-`<target>.lock` DOS — planted symlink permanently blocks activate | v0.8.4 pre-commit adversarial |
| CR-R4 / cleanup | ERR_ACTIVATE_FAILED unreachable defensive fallback | v0.8.4 pre-commit correctness |
| adv-r6-1 (this round) | docs drift: ADR-0014 + AGENT-CLI-CONTRACT referenced removed constant | v0.8.5 pre-commit adversarial |
| correctness-2 (this round) | invariant after defensive-fallback removal not asserted | v0.8.5 pre-commit correctness |
| correctness-3 (this round) | symlink-at-lock test did not pin the v0.8.5 guard wording | v0.8.5 pre-commit correctness |

Total: **456 / 456 tests passing** (v0.8.4 455 → v0.8.5 456 net +1).

### Memory rule (session discipline)

`feedback_pre_stop_audit.md` added to agent memory: before declaring a
session done, audit every "deferred" item against this binary test:

- **Genuine defer**: requires new ideation, a real ADR-worthy decision,
  or external input
- **Session-fatigue defer**: mechanical ~30 LOC change with no genuine
  alternatives — agent is just tired

Session-fatigue defers must be closed this round. Across the v0.8.x
cycle (6 rounds: ideation → v0.8.4 → v0.8.5) I declared stop 6 times;
every time the user pushed back and caught 2-5 mechanical items I'd
defaulted away from. The new rule plus the existing ADR-0013 husky
gate (code-quality) plus `feedback_review_before_push.md` (review
timing) constitute the complete session-discipline triad.

### Deferred (round-7 candidates)

| Item | Why genuine defer |
|---|---|
| adv-009 deactivate verb | New feature; needs ideation for cleanup semantics + state model |
| CI bless path | Real design choice — server-side hook vs encrypted marker vs `--no-verify` policy |
| 3 ADR drafts (Validator Rule Registry / Migration Ladder / contentPipeline) | ADR-0001 D1 implementation-time |
| TOCTOU window between lstat and lockSync | Real design — `O_NOFOLLOW` is Linux-only; cross-platform strategy needed |

4 items left in backlog, **all genuinely deferred** (pre-stop audit passes).
Session-fatigue bucket empty.

### Notes

- v0.8.5 push uses `npm run release:bless -- v0.8.5` (ADR-0013 gate dogfooding round 3 — third consecutive clean ship through the gate)
- ADR-0015 supersedes nothing; ADR-0014 stays as the v0.8.4 record. v0.8.5 is an amend
- 6 releases in this session (v0.8.0/8.1/8.2/8.3/8.4/8.5); the last three (v0.8.3/8.4/8.5) all shipped with a real pre-commit review pass
