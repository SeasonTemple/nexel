# Greeter notes

Multi-file payload fixture: proves the vendor tool copies `references/**`
alongside `SKILL.md` and that the tool-private payload hash spans every file
in the skill directory (not just `SKILL.md`).
