#!/usr/bin/env sh
# skillctl repo pre-commit: run the full test suite. The repo no longer
# ships a top-level skill catalog, so lint:skills / lint:manifest /
# lint:drift only apply when a downstream product wires them up — they
# are not run here.
npm test --silent
